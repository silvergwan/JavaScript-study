let str = "안녕";
let str2 = "안녕하세요, 반갑습니다.";
let str3 = `은관아 ${str}`;

// 자바스크립트에서는 문자열을 따옴표로 가둡니다.

// 따옴표는 세 종류가 있습니다.

// 큰따옴표: "Hello"
// 작은따옴표: 'Hello'
// 역 따옴표(백틱, backtick): `Hello`

// 큰따옴표와 작은따옴표는 ‘기본적인’ 따옴표로, 자바스크립트에서는 이 둘에 차이를 두지 않습니다.

// 역 따옴표로 변수나 표현식을 감싼 후 ${…}안에 넣어주면, 
// 아래와 같이 원하는 변수나 표현식을 문자열 중간에 손쉽게 넣을 수 있습니다.