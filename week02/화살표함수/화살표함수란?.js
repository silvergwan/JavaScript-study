// 함수 표현식
const transfer = function (amount) {
  return amount + "원 송금";
};

// 화살표 함수
const transferr = (amount) => amount + "원 송금";

// 인자 하나면 괄호도 생략
const transferrr = (amount) => amount + "원 송금";
