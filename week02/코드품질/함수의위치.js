// 헬퍼 함수를 사용하는 함수의 위치를 맨 위로 끌어올리고 아래에 헬퍼 함수 선언

// 헬퍼 함수를 사용하는 코드
let elem = createElement();
setHandler(elem);
walkAround();

// --- 헬퍼 함수 ---
function createElement() {
  //   ...
}

function setHandler(elem) {
  //   ...
}

function walkAround() {
  //   ...
}
